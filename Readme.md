# Яндекс.Отзывы

Веб-приложение для просмотра отзывов организаций из Яндекс.Карт.  
Позволяет пользователям авторизоваться, сохранить ссылку на свою организацию и просматривать отзывы, рейтинг и количество отзывов.  
Реализовано на **Laravel 11** (backend API) и **Vue 3** (frontend SPA).  
Запускается в Docker, не требует установки дополнительного ПО (кроме Docker и Git).
## 📋 Требования к системе

Для запуска проекта вам понадобятся всего две программы — они бесплатны и работают на Windows, macOS и Linux.

| Программа | Для чего нужна | Где скачать |
|-----------|----------------|-------------|
| **Docker** | Запускает все компоненты приложения (базу данных, сервер, сайт) в изолированных контейнерах. Без Docker приложение не запустится. | [Официальный сайт Docker](https://www.docker.com/products/docker-desktop/) |
| **Git** | Позволяет скачать (клонировать) код проекта с GitHub. | [git-scm.com](https://git-scm.com/downloads) |

> 💡 **Важно**: После установки Docker обязательно **запустите Docker Desktop** и дождитесь, пока в трее (или строке меню) появится зелёный значок — это означает, что Docker работает.

Если вы не уверены, установлены ли у вас эти программы, выполните в терминале (командной строке):
```bash
docker --version
git --version
```
Если они установлены, вы увидите номера версий.

## 🚀 Установка и первый запуск

Выполните следующие шаги строго по порядку — это займёт не более 5–10 минут.

### 1️⃣ Откройте терминал (командную строку)

- **Windows**: нажмите `Win + R`, введите `cmd` и нажмите Enter. Или найдите «Командная строка» в меню «Пуск».
- **macOS**: нажмите `Cmd + Пробел`, введите `Терминал` и запустите.
- **Linux**: обычно терминал открывается сочетанием `Ctrl + Alt + T`.

В терминале вы будете вводить команды — их нужно копировать целиком и нажимать Enter после каждой.

### 2️⃣ Скачайте проект с GitHub (клонируйте репозиторий)

Вставьте в терминал эту команду и нажмите Enter:

```bash
git clone https://github.com/UnDeadWindAngel/yandex-maps-reviews.git
```

### 3️⃣ Перейдите в папку проекта

```bash
cd yandex-maps-reviews
```

### 4️⃣ Создайте файлы с настройками (переменные окружения)

Проект использует два файла .env — один для Laravel (бэкенд) и один для Docker Compose (корневой).
Скопируйте примеры командой (скопируйте и вставьте целиком):

```bash
cp backend/.env.example backend/.env && cp .env.example .env
```

Что это даёт?
В папке backend появится файл .env с настройками базы данных, сессий и API. В корне проекта появится .env с портами и общими параметрами.
Оба файла уже заполнены безопасными значениями по умолчанию — ничего менять не нужно, если вы запускаете проект локально.

### 5️⃣ Запустите приложение в Docker

Выполните команду:

```bash
docker-compose -f docker-compose.prod.yml up -d
```

⏳ Первый запуск может занять до 10 минут — Docker скачивает базовые образы (PHP, PostgreSQL, Nginx) и собирает контейнеры. Последующие запуски будут мгновенными.

После завершения вы увидите сообщение Starting ... done.
Все контейнеры работают в фоновом режиме.

### 6️⃣ Создайте таблицы в базе данных (миграции)

Внутри контейнера нужно выполнить команду Laravel для создания таблиц:

```bash
docker-compose -f docker-compose.prod.yml exec backend-php php artisan migrate
```
Если команда отработала успешно, вы увидите список созданных таблиц.

### 7️⃣ Откройте приложение в браузере

Перейдите по адресу: http://localhost

Если вы изменили порт в .env (переменная APP_PORT), используйте свой порт, например http://localhost:8080.

Готово! Вы должны увидеть главную страницу с предложением войти или зарегистрироваться.

### 🛑 Как остановить приложение

Если нужно полностью остановить контейнеры (например, чтобы освободить ресурсы), выполните:

```bash
docker-compose -f docker-compose.prod.yml down
```

Все данные останутся в специальных томах Docker и не пропадут.

## 🎯 Как пользоваться приложением

После успешного запуска и открытия в браузере вы попадёте на страницу входа.  
Вот краткая инструкция для конечного пользователя (ваших сотрудников или заказчиков).

### 1️⃣ Регистрация или вход

- Если у вас ещё нет аккаунта, нажмите «Регистрация» и заполните форму:
    - **Имя** (как вас называть)
    - **Email** (используется для входа)
    - **Пароль** (минимум 8 символов) и его подтверждение
- Если аккаунт уже есть, просто войдите.

После успешного входа вы попадёте на домашнюю страницу с навигацией.

### 2️⃣ Настройка организации (самый важный шаг)

Перейдите в раздел **«Настройки»** через меню вверху.

- В поле вставьте **ссылку на карточку вашей организации в Яндекс.Картах**.  
  Пример правильной ссылки:  
  `https://yandex.ru/maps/org/abc123/`  
  (это реальная ссылка, которую вы видите в адресной строке, когда открываете организацию в Яндекс.Картах)
- Нажмите **«Сохранить»**. Если ссылка корректна, система запомнит её и покажет идентификатор организации.

> ⚠️ **Важно:** Ссылка должна вести именно на карточку организации, а не на поиск или другой раздел. Если вы не уверены, скопируйте её из адресной строки браузера, когда находитесь на странице организации.

### 3️⃣ Просмотр отзывов

Перейдите в раздел **«Отзывы»**.  
Вы увидите:

- **Рейтинг компании** (средняя оценка, например 4.7) и **общее количество отзывов**.
- **Список отзывов** по 5 штук на странице. Каждый отзыв содержит:
    - Имя автора
    - Текст
    - Оценку (количество звёзд)
    - Дату
    - Фото (если есть — показывается заглушка)
- Внизу страницы — кнопки пагинации **«Назад»** и **«Вперёд»**, а также номер текущей страницы.

### 4️⃣ Выход из системы

Чтобы выйти, нажмите **«Выйти»** в меню. После этого вы будете перенаправлены на страницу входа.

---

**Готово!** Приложение полностью готово к использованию. Все отзывы и рейтинг генерируются автоматически (режим «fake»).

## ❓ Решение возможных проблем

Если при запуске или использовании приложения возникают ошибки, воспользуйтесь этим руководством.

### 1️⃣ Контейнеры не запускаются, ошибка «port is already allocated»

**Причина:** Указанный в файле `.env` порт (по умолчанию 80) уже занят другой программой (например, Skype, веб-сервером или другим Docker-контейнером).

**Решение:**
- Откройте файл `.env` в корне проекта.
- Найдите строку `APP_PORT=80` и измените её на свободный порт, например `APP_PORT=8080`.
- Сохраните файл и перезапустите контейнеры:
  ```bash
  docker-compose -f docker-compose.prod.yml down
  docker-compose -f docker-compose.prod.yml up -d
  ```
- Теперь приложение будет доступно по адресу http://localhost:8080.

### 2️⃣ После входа сразу выбрасывает на страницу логина (сессия не сохраняется)

**Причина:** Неправильные настройки сессии или `Sanctum`.

**Решение:**
- Убедитесь, что в файле `backend/.env` указан правильный `SANCTUM_STATEFUL_DOMAINS`.
  
  Для локального запуска должно быть:
  ```bash 
  SANCTUM_STATEFUL_DOMAINS=localhost
  ```
  (без порта, так как приложение работает на 80 порту, или с портом, если вы его изменили).
- Проверьте, что в том же файле SESSION_DOMAIN установлен как localhost или вообще удалён.
- Перезапустите контейнеры после изменений:
```bash 
docker-compose -f docker-compose.prod.yml restart backend-php
```

### 3️⃣ При сохранении ссылки на организацию выдаётся ошибка 500

**Причина:** Неверный формат ссылки или проблемы с подключением к базе данных.

**Решение:**
- Убедитесь, что ссылка начинается с https://yandex.ru/maps/org/... или аналогичного домена (yandex.by, yandex.kz и т.д.).
- Проверьте, выполнены ли миграции базы данных:
```bash 
docker-compose -f docker-compose.prod.yml exec backend-php php artisan migrate
```
- Посмотрите логи бэкенда:
```bash
docker-compose -f docker-compose.prod.yml exec backend-php tail -f storage/logs/laravel.log
```
и найдите конкретную ошибку.

### 4️⃣ Страница отзывов пустая или показывает не те данные

**Причина:** У организации ещё нет сохранённой ссылки, или используется драйвер fake, который генерирует данные на основе org_id.

**Решение:**
- Убедитесь, что в разделе «Настройки» сохранена корректная ссылка.
- Если ссылка есть, но отзывов нет — возможно, в мок-режиме для данной организации сгенерировалось нулевое количество. Попробуйте сохранить другую ссылку (с другим org_id).

### 5️⃣ Ошибка «403 CSRF token mismatch»

**Причина:** Проблемы с отправкой CSRF-токена при запросах из SPA. Обычно это связано с неверной настройкой Sanctum.

**Решение:**
- Проверьте, что в backend/.env правильно указан SANCTUM_STATEFUL_DOMAINS (см. п. 2).
- Убедитесь, что в запросах к API присутствует заголовок X-XSRF-TOKEN. В современных браузерах он должен автоматически подставляться из куки.
- Если ошибка возникает при выходе из системы, добавьте в authStore.logout() вызов /sanctum/csrf-cookie перед запросом (в текущем коде это уже есть).

### 6️⃣ Не могу войти: «Предоставленные учетные данные неверны»

**Причина:** Неправильный email или пароль, либо пользователь не зарегистрирован.

**Решение:**
- Попробуйте зарегистрироваться заново или восстановить пароль (функция восстановления не реализована, поэтому лучше создать нового пользователя).
- Если вы уверены, что данные верны, проверьте в базе данных, существует ли такой пользователь:
```bash
docker-compose -f docker-compose.prod.yml exec backend-php php artisan tinker
>>> App\Models\User::where('email', 'ваш@email')->first();
```
Если пользователь не найден, зарегистрируйтесь.

### 7️⃣ Docker не запускается на Windows: «Hardware assisted virtualization and data execution protection must be enabled»

**Причина:** В BIOS не включена виртуализация.

**Решение:**
- Перезагрузите компьютер, войдите в BIOS (обычно клавиша F2, F10, Del) и включите опцию виртуализации (Intel VT-x или AMD-V).
- Сохраните настройки и загрузитесь снова. После этого Docker должен заработать.

### 8️⃣ Не могу скопировать команды из README (копируются с символами $ или лишними пробелами)

**Решение:** Выделяйте только текст команды, исключая символы приглашения (например, $ или >). Либо используйте кнопку копирования в правом верхнем углу блока кода (если поддерживается).

## 👨‍💻 О разработчиках

Проект создан **UnDeadWindAngel**.  
По всем вопросам, предложениям или при возникновении проблем вы можете написать на email:

📧 **[UnDeadWindAngel@yandex.ru](mailto:UnDeadWindAngel@yandex.ru)**

Будем рады обратной связи!

---

## 📄 Лицензия

Данный проект распространяется под лицензией **MIT**.  
Это означает, что вы можете свободно использовать, модифицировать и распространять код, но с указанием авторства и без каких-либо гарантий.

Полный текст лицензии доступен в файле [LICENSE](LICENSE) (если он добавлен в репозиторий) или на сайте [choosealicense.com](https://choosealicense.com/licenses/mit/).

---

**Спасибо, что пользуетесь Яндекс.Отзывами!** 🎉